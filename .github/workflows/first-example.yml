name: My First Work Flow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Welcome message
          run: echo "My first git actions job"
        
        - name: List files
          run: ls
        - name: read file
          run: cat read.md

